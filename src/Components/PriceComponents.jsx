import React from "react";
import { product } from "./ProductComponents";
const Price = () => {
    return(
        <div>
            <p> {product.price} </p>
        </div>
    );
};

export default Price;